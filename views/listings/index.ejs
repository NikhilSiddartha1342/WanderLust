

<!-- this is a boilerplate code written in folder layouts file boilerplate
all pages where we want to use same things again and again we can define that in boilerplate and include that to use that -->

<% layout("/layouts/boilerplate") %>
<!-- when we create new listing a flash mesage appear like new listing is created using flash-message which we defines middleware and here we are printing using its name whihc we defined when creating middleware -->
<!--< %= success %> we are definign this in bioler plate so we can style it-->


<!-- 
<style>
    #filters {
        /* to write in same line display flex and flex wrap as wrap */
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        /* idi endhuku vadamu ante tax-toggle daggara ah tax-toggle block piana untundi adi majjilo undadniki parent container lo unna items anni center chethunam */
    }

    .filter {
        text-align: center;
        margin-right: 2rem;
        margin-top: 2rem;
        opacity: 0.7;
    }

    .filter :hover {
        opacity: 1;
        cursor: pointer;
    }

    .filter p {
        font-size: 0.8rem;
    }

    .tax-info {
        display: none;
    }

    .tax-toggle {
        border: 1px solid black;
        border-radius: 1rem;
        height: 3.25rem;
        padding: 1rem;
        margin-left: 2rem;
        display: flex;
        align-items: center;
    }

</style> -->




    <!-- <h3>All Listings</h3> -->
     <!-- <div id="filters">
        <div class="filter">
           <div><i class="fa-solid fa-fire"></i></div> 
            <p>Trending</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-bed"></i></div> 
            <p>Rooms</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-mountain-city"></i></div> 
            <p>Iconic Cities</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-mountain-sun"></i></div> 
            <p>Mountains</p>
        </div>
        <div class="filter">
           <div><i class="fa-brands fa-fort-awesome"></i></div> 
            <p>Castles</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-person-swimming"></i></div> 
            <p>Amazing Pools</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-campground"></i></div> 
            <p>Camping</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-cow"></i></div> 
            <p>Farms</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-snowflake"></i></div> 
            <p>Arctic</p>
        </div>
         <div class="filter">
           <div><i class="fa-solid fa-igloo"></i></div> 
            <p>Domes</p>
        </div>
        <div class="filter">
           <div><i class="fa-solid fa-ship"></i></div> 
            <p>Boats</p>
        </div>
        
        <div class="tax-toggle">
            <div class="form-check form-switch form-check-reverse">
                <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
                <label class="form-check-label" for="switchCheckDefault">Display total after taxes</label>
            </div>
        </div>
     </div> -->
    <br>
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
        <% for (let listing of allListings) { %>
        <a href="/listings/<%= listing._id %>" class="listing-link">
            <div class="card listing-card" style="width: 20rem;">
                <img
                    class="card-img-top"
                    src="<%= listing.image.url  %>"
                    alt="Listing Image"
                    style="height:20rem"
                />
                <div class="card-body">
                    <p class="card-text">
                        <b><%= listing.title %></b><br>
                        ₹<%= listing.price ? listing.price.toLocaleString("en-IN") : 'Price not set' %> / night
                        <!-- <i class="tax-info"> &nbsp;&nbsp;+&nbsp;&nbsp; 18% GST </i> -->
                    </p>
                </div>
            </div>
            <br><br>
        </a>
        <% } %>
    </div>

<!-- 
    <script>
        //for event listeners by checkbox from add taxes
        //add this script tag code to js folder in public folder
        let taxSwitch = document.getElementById("switchCheckDefault");
        taxSwitch.addEventListener("click", ()=> {
            let taxInfo = document.getElementsByClassName("tax-info");//tax-info is not one element.It is prsent in loop so group of elements hence we sould define with class not id
            for(let info of taxInfo) {
                if(info.style.display != 'inline') {
                    info.style.display = "inline";
                } else info.style.display = "none";
            }

        })
    </script> -->